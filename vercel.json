{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "devCommand": "pnpm build && serve dist/client",
  "buildCommand": "pnpm build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/ssr"
    }
  ]
}
